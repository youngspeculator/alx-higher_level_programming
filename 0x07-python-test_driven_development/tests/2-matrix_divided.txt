Using 2-matrix_divided.py

#Run test with python3 -m doctest -v ./tests/2-matrix_divided.py

This is a module which defines a matrix division function `2-matrix_divided(matrix, div)`
```matrix_divided()``` essentially returns a matrix which is a copy of the parameter
                        `matrix` with all elements divided by `div`

Testing for a standard matrix & positive-value div:
        >>> matrix_divided = __import__('2-matrix_divided').matrix_divided
        >>> matrix = [[3, 12, 6], [24, 39, 15]]
        >>> print(matrix_divided(matrix, 3))
        [[1.0, 4,0, 2.0], [8.0, 13.0, 5.0]]

Testing for standard matrix with floating-point numbers:
        >>> matrix = [[1.2, -2.1, 3.3], [4.2, 5.5, -6.6]]
        >>> print(matrix_divided(matrix, 3))
        [[0.4, -0.7, 1.1], [1.4, 1.83, -2.2]]

Testing for standard matrix with both floats and ints:
        >>> matrix = [[1, 2.4, -3, -4.4, 5], [-6.6, 8.2, -10, 9.4]]
        >>> print(matrix_divided(matrix, 2))
        [[0.5, 1.2, -1.5, -2.2, 2.5], [-3.3, 4.1, -5, 4.7]]

Testing for a standard matrix & a zero-value div:
        >>> matrix = [[1, 2, 3], [4, 5, 6]]
        >>> print(matrix_divided(matrix, 0))
        Traceback (most recent call last):
        ...
        ZeroDivisionError: division by zero

Testing for a standard matrix with a non-integer value:
        >>> matrix = [[1, "ALX", 3], [4, 5, 6]]
        >>> print(matrix_divided(matrix, 2))
        Traceback (most recent call last):
        ...
        TypeError: matrix must be a matrix (list of lists) of ints/floats

Testing for a standard matrix & a non-integer div:
        >>> matrix = [[1, 2, 3], [4, 5, 6]]
        >>> print(matrix_divided(matrix, "string"))
        Traceback (most recent call last):
        ...
        TypeError: div must be a number

Testing for a matrix which is not a `list of lists`
        >>> matrix = [1, 2, 3]
        >>> print(matrix_divided(matrix, 2))
        Traceback (most recent call last):
        ...
        TypeError: matrix must be a matrix (list of lists) of ints/floats

Testing for a matrix with rows of different sizes
        >>> matrix = [[1, 2, 3, 4], [5, 6, 7]]
        >>> print(matrix_divided(matrix, 3))
        Traceback (most recent call last):
        ...
        TypeError: each row of the matrix must have the same size
